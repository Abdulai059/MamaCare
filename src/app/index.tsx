import React from "react";
import { View } from "react-native";

/**
 * Index Route
 * This route is deprecated - routing is now handled by the root layout
 * Returns empty view
 */
export default function IndexScreen(): React.JSX.Element {
  return <View className="flex-1" />;
}
